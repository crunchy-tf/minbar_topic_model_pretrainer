sentence-transformers>=2.2.0
bertopic[visualization]>=0.15.0 # [visualization] is optional but useful for inspecting topics
pandas>=1.3.0
loguru>=0.7.0
requests # For wikipedia_downloader.py
gensim>=4.0.0 # ADDED for Wikipedia parsing

# For CPU-only PyTorch
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
# torchvision --index-url https://download.pytorch.org/whl/cpu # Optional
# torchaudio --index-url https://download.pytorch.org/whl/cpu  # Optional

# Removed: wikiextractor, Babel, pylatexenc, beautifulsoup4 (unless used elsewhere, unlikely)